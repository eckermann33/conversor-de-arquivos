# Projeto para o XcodeGen: `xcodegen generate` cria o ConvrtVideo.xcodeproj.
# Assim o .xcodeproj não precisa ficar versionado (ele muda a cada abertura do Xcode).
name: ConvrtVideo

options:
  bundleIdPrefix: com.convrt
  deploymentTarget:
    macOS: "13.0"
  createIntermediateGroups: true
  groupSortPosition: top

settings:
  base:
    SWIFT_VERSION: "5.0"
    MARKETING_VERSION: "1.0"
    CURRENT_PROJECT_VERSION: "1"
    CODE_SIGN_IDENTITY: "-"
    CODE_SIGN_STYLE: Automatic
    ENABLE_HARDENED_RUNTIME: NO
    DEAD_CODE_STRIPPING: YES
    SWIFT_TREAT_WARNINGS_AS_ERRORS: NO
    # Xcode 26 passou a marcar projetos novos com isolamento padrão na
    # main actor ("Approachable Concurrency"). Este app gerencia threads
    # na mão (DispatchQueue.global para o ffmpeg, .main para atualizar a
    # UI), então fixamos o comportamento antigo para não depender de
    # inferência de concorrência que não foi revisada para o Swift 6.
    # Configurações desconhecidas em versões mais antigas do Xcode são
    # ignoradas sem erro, então isso é seguro em qualquer versão.
    SWIFT_DEFAULT_ACTOR_ISOLATION: nonisolated
    SWIFT_APPROACHABLE_CONCURRENCY: NO
    SWIFT_STRICT_CONCURRENCY: minimal

targets:
  ConvrtVideo:
    type: application
    platform: macOS
    sources:
      - path: ConvrtVideo
        excludes:
          - "Resources/Info.plist"
          - "Resources/ConvrtVideo.entitlements"
          - "Resources/.gitkeep"
    settings:
      base:
        PRODUCT_NAME: ConvrtVideo
        PRODUCT_BUNDLE_IDENTIFIER: com.convrt.video
        INFOPLIST_FILE: ConvrtVideo/Resources/Info.plist
        CODE_SIGN_ENTITLEMENTS: ConvrtVideo/Resources/ConvrtVideo.entitlements
        # O app precisa executar o ffmpeg como subprocesso; a caixa de areia
        # do macOS bloqueia isso, então ela fica desligada (app pessoal, fora da App Store).
        ENABLE_APP_SANDBOX: NO
        COMBINE_HIDPI_IMAGES: YES
        LD_RUNPATH_SEARCH_PATHS: "$(inherited) @executable_path/../Frameworks"
